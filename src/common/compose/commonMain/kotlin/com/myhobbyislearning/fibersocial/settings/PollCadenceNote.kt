package com.myhobbyislearning.fibersocial.settings

/**
 * One-line caption under the poll-cadence setting saying when checks actually happen —
 * platform-specific because the honest answer differs (issue #113 picked qualitative
 * cadence labels for exactly this reason).
 */
internal expect val pollCadencePlatformNote: String
