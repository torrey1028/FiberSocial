<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <!-- USE_EXACT_ALARM exists only on API 33+; Android 12/12L needs the revocable
         SCHEDULE_EXACT_ALARM or setExactAndAllowWhileIdle throws SecurityException. -->
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- Required on API 30+ to query for browser apps that handle HTTPS URLs -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>

    <application
        android:name=".FiberSocialApplication"
        android:label="FiberSocial"
        android:theme="@style/Theme.FiberSocial">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <receiver android:name=".notifications.ReminderReceiver" android:exported="false"/>
        <receiver android:name=".notifications.BootReceiver" android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
            </intent-filter>
        </receiver>
        <!--
            AppAuth's RedirectUriReceiverActivity is merged in from the appauth library
            with the fibersocial:// scheme via the appAuthRedirectScheme manifest placeholder.
            It receives the OAuth callback and delivers the result back to the launcher.
        -->
    </application>
</manifest>
